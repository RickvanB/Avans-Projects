// import app from '../src/app';
// import chai from 'chai';
// import chaiHttp = require('chai-http');
// import 'mocha';

// chai.use(chaiHttp);
// const expect = chai.expect;

// describe('The specs for the majors endpoint of the Speedmeet API', () => {
    
//     it('GET /majors should return all majors', function(done){
//         chai.request(app)
//             .get('/majors')
//             .end(function(err, res){
//                 expect(err).to.be.null;
//                 expect(res).to.have.status(200);
//                 expect(res.body).to.not.be.undefined;
//                 //Check if the first object in the array has keys of Company
//                 expect(res.body[0]).to.have.keys(['id', 'name', 'maxEnrollmentsPerCompany']);
//                 done();
//             });
//     });

    
// });

